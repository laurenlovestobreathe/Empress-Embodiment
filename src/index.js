import React from 'react';
import ReactDOM from 'react-dom/client';
import EmpressEmbodimentBot from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <EmpressEmbodimentBot />
  </React.StrictMode>
);
